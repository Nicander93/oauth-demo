server:
  port: 8090

spring:
  application:
    name: oauth-resource
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://127.0.0.1:9000

logging:
  level:
    org.springframework.security: DEBUG
